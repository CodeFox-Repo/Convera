/**
 * Global application configuration
 */
export const appConfig = {
  window: {
    // Default bottom margin as percentage of screen height
    defaultBottomMarginPercent: 3,
    // Fallback pixel margin if percentage calculation fails
    fallbackBottomMarginPixels: 100,
  },
};
