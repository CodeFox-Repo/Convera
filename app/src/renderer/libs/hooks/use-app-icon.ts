/**
 * Hook to get app icon for a given app name
 * Simplified version that always returns null (icons disabled)
 */
export function useAppIcon() {
  // Icons are disabled, always return null
  return null;
}
