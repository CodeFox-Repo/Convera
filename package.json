{
  "name": "foxfoxy-workspace",
  "private": true,
  "version": "0.0.1",
  "description": "FoxyFox Workspace",
  "scripts": {
    "start": "cd app && pnpm start",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "format:write": "pnpm -r format:write",
    "test": "pnpm -r test",
    "test:all": "pnpm -r test:all"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.1"
}